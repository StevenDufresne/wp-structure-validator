{
  "name": "WordPress-Theme-Check-Action",
  "version": "1.0.0",
  "description": "Run Theme Check test for a WordPress theme",
  "main": "index.js",
  "scripts": {
    "wp-env": "wp-env",
    "activate-theme" : "npm run wp-env run cli theme list && npm run wp-env run cli theme activate test-theme",
    "theme-check": "npm run wp-env run cli eval-file ./config/check-theme.php"
  },
  "author": "The WordPress Contributors",
  "license": "GPL-2.0-or-later",
  "dependencies": {
    "@wordpress/env": "^2.0.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.3"
  }
}
